import { BaseFormComponentDirective } from './base-form-component.directive';

describe('BaseFormComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFormComponentDirective();
    expect(directive).toBeTruthy();
  });
});
