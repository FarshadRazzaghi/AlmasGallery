import { BaseDeleteModalDirectiveDirective } from './base-delete-modal-directive.directive';

describe('BaseDeleteModalDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseDeleteModalDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
